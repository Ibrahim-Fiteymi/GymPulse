[project]
name = "gympulse-api"
version = "0.1.0"
description = "Gym equipment analytics, reservations, and busy-hour prediction API"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "sqlmodel>=0.0.22",
  "pydantic>=2.7.0",
]

[tool.uv]
dev-dependencies = []
